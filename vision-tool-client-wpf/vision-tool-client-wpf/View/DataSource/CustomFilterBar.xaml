<UserControl x:Class="vision_tool_client_wpf.View.DataSource.CustomFilterBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:vision_tool_client_wpf.View.DataSource"
             MinHeight="60" Background="Transparent"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    

    <!-- 배치: 가로 플로우 -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Line -->
        <ComboBox Grid.Column="0" 
                  Text="Line"/>

        <!-- AIResult (+2 뱃지 예시) -->
        <ComboBox Grid.Column="2" 
                  Text="AI 판정"/>

        <!-- ManualResult (+1 뱃지 표현 원하면 Badge Text를 +1로 교체) -->
        <ComboBox Grid.Column="4" 
                  Text="작업자 판정"/>

        <!-- More Filters -->
        <Button Grid.Column="6" 
                Content="더 보기"/>
    </Grid>

</UserControl>
